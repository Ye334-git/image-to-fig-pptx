# 日志
*.log

# 临时草稿脚本（dot 前缀的一次性脚本不入库）
.*.ps1
.*.cjs

# 依赖
node_modules/

# 构建产物
.codex-build/
tools/html-to-pptx/.work/
tools/image-to-html/dist/

# 密钥与凭据 —— 任何位置都不入库
.env
.env.*
*.local-provider-config.json
**/.image-to-html-data/
**/.image-to-slice-history/
**/.image-to-slice-data/

# 本地数据与历史
tools/image-to-html/.image-to-html-data/
image-to-slice/.image-to-slice-history/
image-to-slice/.local-provider-config.json

# 测试产物
tools/image-to-html/test-results/

# 大体积可再生交付物（保留在磁盘，不入库）
deliverables/

# README 对比图的全分辨率原图：README 用的是 docs/images 里压缩过的版本，
# 原图保留在磁盘上以便日后重新导出，但不入库（约 7 MB 二进制）。
docs/dev/comparison-originals/
